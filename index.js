// Define variables as per the instructions

// Equation that multiplies num1 and num2 to get 62
const multiply = 31 * 2;

// Function to generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;

// Equation that calculates the remainder of num3 divided by num4 to be 4
const mod = 14 % 10;

// Finding the highest number in the set
const numbers = [5, 10, 15, 20, 3];

const max = Math.max(...numbers);